const ProceedButton = ({ text }) => {
  return (
    <div className="uppercase my-5 flex items-center justify-center bg-orange-500 h-14 rounded-lg text-white font-bold text-xl">
      {text}
    </div>
  );
};

export default ProceedButton;
